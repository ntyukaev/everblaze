import { Layout } from 'antd'

const { Header } = Layout

const TopMenu = () => {
  return (
    <Header
      className="site-layout-background"
      style={{
        padding: 0,
      }}
    />
  )
}

export default TopMenu
