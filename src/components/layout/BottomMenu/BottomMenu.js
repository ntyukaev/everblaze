import { Layout } from 'antd'

const { Footer } = Layout

const BottomMenu = () => {
  return (
    <Footer
      style={{
        textAlign: 'center',
      }}
    >
      Footer
    </Footer>
  )
}

export default BottomMenu
